plugins {
    id 'com.android.library'
    id 'kotlin-android'
    id 'kotlin-kapt'
    id 'kotlinx-serialization'
}

android {
    buildTypes {
        debug {
            buildConfigField 'String', 'API_ENDPOINT', '"https://example.com/"' // debug server
        }
        staging {
            consumerProguardFiles 'proguard-rules.pro'
            buildConfigField 'String', 'API_ENDPOINT', '"https://example.com/"' // stage server
        }
        release {
            consumerProguardFiles 'proguard-rules.pro'
            buildConfigField 'String', 'API_ENDPOINT', '"https://example.com/"' // release server
        }
    }
}

dependencies {
    implementation project(':domain')
    implementation project(':resources')

    // Desugaring
    coreLibraryDesugaring "com.android.tools:desugar_jdk_libs:$desugaringVersion"

    // JSON serialization
    api "org.jetbrains.kotlinx:kotlinx-serialization-json:1.3.0"

    // Retrofit 2
    def retrofitVersion = '2.9.0'
    api "com.squareup.retrofit2:retrofit:$retrofitVersion"
    api "com.jakewharton.retrofit:retrofit2-kotlinx-serialization-converter:0.8.0"

    // OkHttp 3
    def okhttpVersion = '4.9.1'
    api "com.squareup.okhttp3:okhttp:$okhttpVersion"
    api "com.squareup.okhttp3:logging-interceptor:$okhttpVersion"

    // Shared preferences
    api 'com.github.tfcporciuncula.flow-preferences:flow-preferences:1.3.2'
}